#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: print all  single digit numbers starting from zero
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		putchar('n');
		++n;
	}
	putchar('\n');

	return (0);
}
